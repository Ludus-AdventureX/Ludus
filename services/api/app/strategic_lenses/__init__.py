"""Strategic lens runtime behavior modules.

Each lens module owns only its own behavior validation. Canonical schemas,
persistence, API routes, and the shared harness stay with their manifest owners.
"""
