# External fixture boundary

Deterministic model/search/crawl responses may be added here for an explicitly selected fixture fallback. Every response must retain provider and `sourceMode=fixture` provenance and must never impersonate a live call.