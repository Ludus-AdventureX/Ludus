# Work Log: QA — Task 9 analyses HTTP handlers candidate acceptance (r1b)

- Status: COMPLETE on worktree decision-lab-G0/worktrees/qa-task09-analyses-r1b; verdict PASS.
- Role: independent QA lane (session takeover authorized by dispatcher; the parallel QA session's worktree/container/branch untouched — isolated suffix -b resources).
- Branch: `codex/qa-task-09-analyses-http-r1b`, cut from candidate tip `0f92b172c5d931531a1d450254655ed6b958a95f` (codex/task-09-analyses-http-handlers-r1) so the QA tip carries the candidate chain for MOUNT-02's single no-ff adoption. No rebase/amend/force.
- Live main at Gate 0 = `23ab477649716f52b3aa913e5efb209442397efa`; candidate base = 4941e58. Merge-deviation reconciliation is MOUNT-02's job, not QA's (disclosed).
- Scope: own adversarial tests + HEAD/HISTORY only. ZERO touch on app/analyses/routes.py, repository, state machine, models, schemas, migrations, main.py, packages/contracts.
- Verification env: fresh disposable PG16 `ludus-pg-qa-task09-http-b` @55453; main venv/.tools/node_modules via junction; offline, zero installs; alembic single head f9a4b7e2c8d3 applied clean.
- Five-gate results (all on the fresh PG16 @55453):
  1. Independent rerun: own candidate suite 32/0; owner diagnostic `app/analyses/tests app/evidence/tests` 239/0; full `pytest tests app/simulations/tests app/evidence/tests app/analyses/tests -q -W error -rxX` = 816 passed / 0 failed; canonical `pytest tests app/simulations/tests -q -W error -rxX` = 577 passed (authoritative baseline matched). `ruff check services/api` all-pass; `compileall app` exit 0; `git diff --check` clean; official `generate_contracts.ps1 -Check` = CONTRACT_DRIFT_OK (router still UNMOUNTED, published surface stays 8 ops, zero contract leak); secret scan clean (single hit = the `sk-` substring inside the literal branch name `task-09`, false positive).
  2. Adversarial supplement `test_qa_adversarial_mount02_r1b.py` = 8 passed / 0 failed: (A1) key reuse across charters in-workspace -> IDEMPOTENCY_CONFLICT 409, no silent cross-charter replay; (A2) same key across tenants -> fresh run, no cross-tenant replay/leak; (A3) replay-after-cancel returns the terminal run (meta.idempotencyReplay true, status cancelled), no resurrection/duplicate, and a new key then creates a fresh queued run; (A4) body-smuggled Idempotency-Key on cancel -> 422 with details.header; (A5) byte-identical uniform CASE_NOT_FOUND 404 across ALL three new GET endpoints for foreign-tenant vs ghost ids; (A6) PATCH cannot flip status or smuggle non-editable fields (422, state pristine); (A7) hostile payloads (non-object JSON, weaponized enum) degrade into the {ok:false,error} envelope, no 500; (A8) confirm bridge also correct from awaiting_confirmation.
  3. Disclosure adjudications: (a) M9 disclosed maintenance of 7 pre-existing cancel calls = header-only additions, zero assertion-semantics change — LEGITIMATE (and the cancel-1/cancel-2 distinct-key test correctly proves natural idempotency, matching the M9 ruling); (b) M8 CSRF fix-plan written into handoff r4 and NOT self-fixed — CORRECT lane discipline (CSRF is cross-cutting; app/security/csrf.py + main.py are outside the candidate's write domain); recommendation is complete and actionable for MOUNT-02 (require_csrf dependency, SIM-02A parity, canonical-text clarification).
  4. Scope audit `git diff 4941e58...0f92b17 --name-only` = exactly HEAD, HISTORY, handoff r4 addendum, app/analyses/routes.py, own test (new), and the two disclosed test maintenance files. Zero out-of-domain touches; zero base gap files touched.
  5. Handoff section-8/next-step probes: the r4 addendum's MOUNT-02 handover is accurate — analyses router verified carrying 11 routes while reaching no generated contract (CONTRACT_DRIFT_OK proves zero leak); alembic single head f9a4b7e2c8d3 re-verified; the M8 CSRF recommendation cross-checked against app/security/csrf.py double-submit semantics (Origin/Referer + cookie/header constant-time compare) and app/simulations/routes.py L145-156 precedent — dependency-order note holds (auth context resolves before require_csrf, so unauthenticated stays 401).
- Verdict: **PASS**. ready_for_mount: YES — MOUNT-02 may adopt this QA tip (carries candidate 0f92b17) with a single --no-ff merge.
- Secret policy: do not record credentials here.
