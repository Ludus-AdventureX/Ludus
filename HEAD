# Work Log: Task 4+5 backend (dossiers, candidate memory, daily dialogue) r1

- Lane: Case/API/Data Owner (Fable5); branch `codex/task-04-05-backend-r1`; worktree `decision-lab-task04-05-backend-r1`.
- Gate 0: base = remote-verified main `4941e58bee3b91f14a4a92b7fab92750ef85b3b6` (2026-07-25, Asia/Shanghai; `git ls-remote origin refs/heads/main` confirmed).
- Rebuild lineage (principal-approved "backup-then-port"): the prior r1 attempt was a single commit `1ea3925` on the stale base `4508b3059e1a71f62f90bdabcfb7a36cfc50cac4`. Gate 0's "zero-commit" premise did not hold; the commit was preserved as tag `archive/task-04-05-backend-r1-1ea3925` (zero work lost, never pushed to origin), the old worktree/branch were removed, this fresh worktree + same-name branch were rebuilt from the new base, and the prior implementation was ported via `git cherry-pick -n 1ea3925`. No rebase/amend/force.
- New-base adaptation surface (4508b30..4941e58 = 27 commits): `app/models.py`, `app/agents/model_provider.py`, `app/tenancy/routes.py`, `main.py` are byte-identical across the two bases, so the port applied clean (only HEAD/HISTORY conflicted, resolved append-only). Migration chain advanced from head `b2c7e9d4a1f6` to single head `f9a4b7e2c8d3` (via `e7f3a2c9d5b1` evidence-ledger + `f9a4b7e2c8d3` analysis-runtime); the lane's migration-lifecycle test is re-pinned to this new head.
- Delivered: (1) case list/create/read canonical routes (relative router, frozen 10-api shapes; frontend `caseListRouteAvailable` flips at the mounting wave); (2) ProposeEntry/ConfirmEntry/RejectEntry/ExpireEntry/ReclassifyEntry command objects on frozen canonical tables; (3) immutable snapshots (`dossier_versions` + companion `dossier_version_snapshots`); (4) DeepSeek env-only provider + fixture provider + `complete_structured_checked` (empty-content detection, canonical schema validation, single repair retry, no free-text fallback; reasoning_content dropped in-provider, zero persistence/logging); (5) message persistence with provider/model/token metadata + DecisionSubject binding; (6) post-reply candidate extraction writing only `candidate_revisions` (decision questions + options; opt-out -> empty); (7) QuickAnalysisResult from confirmed entries only, permanently non-formal.
- Write scope honored: app/dossiers/**, app/cases/**, app/conversations/**, app/agents/model_provider.py (additive only), migrations/versions/a7c3e9f1b5d8 (the released deferred revision, single new file), own tests, handoff, HEAD/HISTORY. Forbidden domains untouched (evidence/analyses/workers/strategic_lenses/simulations, apps/web, packages/contracts, main.py, models.py, all pre-existing migration files).
- Migration discipline: code-first; deferral RELEASED. A1 (Task 10) reported migration rev-id `b6e8f3a1d7c2` (add_analysis_outputs, after `f9a4b7e2c8d3`); this lane's `a7c3e9f1b5d8_add_dossier_version_snapshots.py` now lands with `down_revision = 'b6e8f3a1d7c2'`, generated from `app/dossiers/models.py` metadata. The parent FILE ships on the A1 branch, so in-worktree the revision has a dangling parent by design (lands last; alembic-applicable only after the integration merge — lifecycle upgrade/downgrade verification is INTEGRATION-WAVE-owned). QA-7 re-pinned to the released state (exact 10-file `versions/` set, chain wiring `a7c3e9f1b5d8` -> `b6e8f3a1d7c2`, expected in-worktree head pair).
- Gates: "PENDING" in the earlier revision of this line referred ONLY to the pre-push re-verification pass on the new base; it was COMPLETED before the port commit `6edeb4a` and the numbers are final in the handoff §Acceptance gates (owner+QA battery 33 passed on migrated disposable PG16; regression smoke 81 passed; ruff/compileall/diff-check/marker/scope/secret scans clean; CONTRACT_DRIFT_OK). Migration follow-up delta (this update) re-verified: QA battery re-run green + ruff clean on touched files.
- Handoff: docs/handoffs/TASK_04_05_BACKEND_R1_HANDOFF.md (mount + migration follow-up instructions inside).
- Next work must start from the latest remote-verified `main`, use a new `codex/<task-name>` branch, and replace this record before modifying task-owned files.
- Secret policy: do not record credentials here.
