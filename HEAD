# Work Log: QA - Task 4/5 Backend r1 (adversarial verification)

- Lane: independent QA (Fable5); branch `codex/qa-task-04-05-backend-r1`; worktree `decision-lab-qa-task0405-backend-r1`.
- Gate 0: candidate = remote-verified `codex/task-04-05-backend-r1` @ `a51a4f9af63312e031e00581635dbf86ca64a3a9` (chain 4941e58 -> 6edeb4a -> a51a4f9, ls-remote exact); A1 reference `codex/task-10-quality-gate-r1` @ `82ee67c` fetched for the scratch-merge bonus only.
- Verdict: PASS (with two QA-owned revisions carried on this tip, see F1/F2; details in docs/handoffs/TASK_04_05_BACKEND_R1_QA_REPORT.md).
- Special constraint honored: NO alembic command was run on the candidate tree (a7c3e9f1b5d8 has a by-design dangling parent b6e8f3a1d7c2 whose file ships on the A1 branch).
- Migration duties: (1) static column-by-column review of a7c3e9f1b5d8 vs app/dossiers/models.py metadata = MATCH (PK, UNIQUE(workspace_id,dossier_version_id), both FKs incl. ondelete=CASCADE, JSONB '[]' server_default, now()/gen_random_uuid() defaults); DB-level cross-check: information_schema columns + pg_get_constraintdef of the migration-built table (scratch merged DB) vs the metadata create_all-built table (fresh DB) = ZERO diff. (2) QA-7 tripwire re-run independently = 2 passed. (3) BONUS scratch merge (local-only worktree, NEVER pushed): 82ee67c + a51a4f9 no-ff temp merge; lifecycle on disposable DB qa0405_merge_lc (one-time PG16 ludus-pg-mainline-w1 @55447 reused): heads = single a7c3e9f1b5d8; upgrade head clean; downgrade -1 = b6e8f3a1d7c2; re-upgrade clean; `alembic check` initially FAILED (F1) and passes after the env.py fix.
- Findings (all resolved on this tip): F1 env.py missed `import app.dossiers.models` -> `alembic check` on the merged chain reports spurious remove_table drift (fix: one import, carried here). F2 tests/test_models.py exact-table-set was selection-order dependent (co-running any dossiers test flipped it; equality KEPT, expected extended with dossier_version_snapshots + deterministic import; repro combo now 38 passed). F3 pre-merge-only: full suite `-W error` turns the alembic dangling-parent UserWarning into 1 failure inside app/evidence/tests/test_evidence_models.py chain-robustness test; on the merged chain the same file = 23 passed -> integration-wave artifact, NOT a candidate defect, no code change.
- Gates (fresh DB qa0405_fresh migrated to f9a4b7e2c8d3 via the scratch tree; QA venv provisioned offline from uv cache, zero installs): owner 33 passed (independent re-run) + regression smoke 81 passed + adversarial battery 12 passed (ADV-1..8: reject/pending negative matrix incl. snapshot payload scan; stale-confirm 409 zero-partial-write; snapshot byte-stability + companion append-only; WRITE-path anti-enumeration byte-identical 404s; opt-out zero candidates AND zero extraction model calls; hostile reasoning_content envelope never persists + information_schema scan; structured-checked repair budget exactly one retry across empty/invalid/empty-empty; fixture determinism under adversarial payloads) + F2 repro combo 38 passed; candidate full suite 829 collected = 828 passed / 1 failed (= F3, adjudicated above).
- Static gates: ruff clean on QA-touched files; compileall clean; git diff --check clean; conflict-marker scan 0 on QA delta; secret scan clean; scope audit: candidate delta 4941e58..a51a4f9 = exactly the declared write scope; QA delta = env.py (F1) + tests/test_models.py (F2) + new tests/test_task0405_qa_adversarial_r2.py + lifecycle/report ONLY.
- CONTRACT_DRIFT_OK: official script's OpenAPI regeneration byte-identical to canonical; TS regen (openapi-typescript 7.13.0 via primary worktree CLI, same two-step precedent as the owner run) byte-identical to canonical types.gen.ts.
- Integration-wave notes: merge order MUST be A1 first (parent file of a7c3e9f1b5d8 lives in A1's chain); expected single post-merge head a7c3e9f1b5d8; env.py will conflict textually with A1's import block - keep BOTH blocks (semantic merge); QA-7 head-pair assertion collapses to single head post-merge exactly as its comment states; F3 disappears once the chain is united (verified).
- Next work must start from the latest remote-verified `main`, use a new `codex/<task-name>` branch, and replace this record before modifying task-owned files.
- Secret policy: do not record credentials here.
