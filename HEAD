# Work Log: Guest Simulation Technical Alpha integration

- Status: full verification gate GREEN on codex/prototype-guest-alpha-integration (base = live-verified remote main 51ae45c900ae4efa01b72d5d6842adb74ad50c91; task-order expectation bd9fde15 is an ancestor already superseded by the prior SIM alpha publication). main NOT advanced: the task's ff-only precondition (remote main re-read == bd9fde15) does not hold.
- Role: Integration / Release Owner (GUEST SIMULATION TECHNICAL ALPHA INTEGRATION).
- Merged (no-ff, in order): run-api 56e01ab (already reachable from main, recorded as no-op), guest-seed-smoke 3278dd8, web-demo d504b4f, deploy-stack c71d66a (sole parent 28511a9 verified; 28511a9 not merged separately). d2ae634/ENG-02 chain not re-merged (already on main).
- Four integration fixes: (1) Run API mounted once under workspace_router (canonical /api/workspaces/{workspaceId}/simulations/{graphId}/runs[/{simulationRunId}]; guest router include_in_schema=False); (2) next.config.ts final semantics = standalone output + API_PROXY_TARGET ?? API_PROXY_ORIGIN + exact /api/health->/health rewrite before generic /api/:path*, trailing slash stripped; deploy-stack Dockerfiles without the syntax frontend line retained; (3) web client normalizes the flat guest bootstrap data into GuestSession.fixture (backend untouched); (4) real HTTP smoke runner app/prototype/smoke_guest_alpha.py (python -m app.prototype.smoke_guest_alpha) replaces the prototype-path fake smoke and is wired into the compose smoke service.
- Gates: fresh PG16 (ludus-pg-guest-alpha @55445) alembic upgrade/heads(single b2c7e9d4a1f6)/check clean; pytest tests app/simulations/tests -q -W error -rxX = 493 passed / 0 failed / 0 xfailed / 0 xpassed; pnpm web test 16 passed + build OK; generate_contracts.ps1 + -Check = CONTRACT_DRIFT_OK (two run paths in OpenAPI, /api/auth/guest absent); ruff/compileall/git diff --check clean; compose config/up: db/api/web healthy, API /health 200, web /api/health 200; REAL smoke over public HTTPS (AUTH_COOKIE_SECURE=true): two isolated guests, same-cookie 200/reused restore, run 201 sim-engine-1.1.0 with valid inputHash, replay identical, cross-guest uniform 404 both directions = SMOKE PASS; /demo browser acceptance: auto guest -> run -> result -> replay -> reload restores the same guest.
- Combination-only extra fixes: guest QA test imports tests.conftest (collection clash), unused repository_module import (ruff), smoke 201/200 bootstrap semantics.
- Flags: ready_for_public_https = YES (public HTTPS smoke already green via temporary tunnel); ready_for_main = YES on content, ff-only push withheld pending adjudication of the stale bd9fde15 baseline expectation.
- Public naming: Simulation Technical Alpha only (no MVP/Production claims).
- Next work must start from the latest local `main`, use a new `codex/<task-name>` branch, and replace this record before modifying task-owned files.
- Secret policy: do not record credentials here.
