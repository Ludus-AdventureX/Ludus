# Work Log: Task 11 Phase 0 five-workspace shell (Session A)

- Status: Session A complete on codex/task-11-phase0-shell-a (base = live-verified remote main 51ae45c900ae4efa01b72d5d6842adb74ad50c91; Gate 0 exact match, no advance); all acceptance gates green (web test 20 passed incl. 14-test baseline zero regression, typecheck/lint clean, build OK with /cases/[decisionCaseId] 11 kB route / 125 kB first load, diff --check + conflict-marker + secret + scope audits clean); handoff at docs/handoffs/TASK11_PHASE0_SHELL_A_HANDOFF.md; Session B must branch from this branch's exact pushed SHA.
- Role: Web/UX Owner (Fable5). Session A of the A/B serial split; Session B must use this session's exact pushed SHA as its only parent.
- Scope: apps/web app/(workspace)/cases/[decisionCaseId] route skeleton, components/shell/DecisionSpine.tsx, components/shell/CaseViewRouter.tsx, structured per-workspace placeholder views (Look V7 static layout frames, no mock analysis data), empty view without a template card wall, component map recorded in docs/handoffs, new component tests (route render, five-workspace switching, empty state, a11y landmarks/focus).
- Look snapshot: all six look sources SHA256-verified byte-identical to frozen design/look-source-manifest.json; index.html used as structure/accessibility reference only; app.js never loaded by production.
- Forbidden this lane: /demo page, guest client, next.config.ts, package.json dependencies, services/api/**, packages/contracts/**, rebase/amend/force-push, parallel IA (e.g. /chat), fabricated analysis/case data.
- Acceptance gates: pnpm --dir apps/web test (baseline 14 passed, zero regression + new tests), pnpm --dir apps/web build, git diff --check, conflict-marker scan, secret scan, scope audit; handoff in docs/handoffs/; push and report exact SHA.
- Next work must start from the latest local `main`, use a new `codex/<task-name>` branch, and replace this record before modifying task-owned files.
- Secret policy: do not record credentials here.
