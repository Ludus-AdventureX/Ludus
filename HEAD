# Work Log: Release integration Task 4/5/9/10/17/18/19 closure (r1)

- Status: ACTIVE on isolated worktree `decision-lab-release-integration-r1`; no mainline write is permitted until every release gate is green.
- Role: Release Integration / Mainline Audit owner. Branch `codex/release-integration-task04-19-r1`, base = live remote main `cacf2a35f892fc22ab8ef21a029667a0053b8342` (verified against `git ls-remote` on July 25, 2026). No rebase/amend/force.
- Scope: dependency/security baseline repairs; independent Task 4/5 QA; ordered Task 10 -> Task 4/5 -> Task 9 integration reconciliation; Case/Conversation/Charter/Run/Report publication; live provider wiring; Signoff + append-only DecisionRecord + Review; independent Playwright golden path; CI and Task 19/17/18 release gates.
- Mainline observation: base already contains MOUNT-02 (`cacf2a3`) with the Task 9 Charter/Run/Lens route publication and CSRF closure; integration will preserve that accepted history and will not replay already-canonical commits.
- Release invariants: no automatic `decided`; DecisionRecord is append-only; no qualifying Run means no Report; Workspace scope on every read/write; no hidden reasoning persistence; official contract generator only.
- Push policy: pin implementation and QA SHAs, require P0/P1 = 0, run migration/contract/Decision OS/static/security/E2E gates, re-read live remote main immediately before push/merge, and never force push.
- Reconciled-tree gates (2026-07-25, disposable PG16 ludus-pg-release-r1 @55470, main venv junction, zero installs): alembic single head c8d4e6f0a1b2 exact; upgrade + check clean -> downgrade -1 = a7c3e9f1b5d8 -> downgrade -1 = b6e8f3a1d7c2 -> re-upgrade head clean -> re-check clean. Full suite `pytest tests app/simulations/tests app/evidence/tests app/analyses/tests -q -W error -rxX` = 937 passed / 0 failed (NEW cross-wave baseline; = 936 READ-01 + 1 lane test). Canonical `pytest tests app/simulations/tests -q -W error -rxX` = 670 passed / 0 failed (NEW baseline, supersedes 669; the lane release-routes test joined the selection). Web: pnpm test = 89/89, typecheck exit 0, production build clean (no turbopack, CI parity). Contracts: official generate_contracts.ps1 on the merged tree, ops 47 -> 56 purely additive (9 decision/signoff routes, zero removals), schemas 67 -> 70 additive, -Check = CONTRACT_DRIFT_OK. Static: ruff app+tests+migrations all-pass; compileall exit 0; git diff --check clean; conflict-marker scan 0; HISTORY zero-deletion vs origin/main verified (one displaced archive header restored, disclosed).
- ready_for_qa: YES. Push to main pending principal confirmation (fail-closed live re-read of origin/main required immediately before any push).
- Reconcile r2 (2026-07-25): pre-push fail-closed re-read caught main advance 465869a -> fdc31d1 (READ-01 web flip: evidence anchors + sandbox data assembly, contracts regen). Merged back --no-ff; conflicts lifecycle-only (contracts auto-merged); HEAD/HISTORY append-only union via the byte-safe resolver, fdc31d1 worklog archived verbatim, HISTORY zero-deletion vs origin/main re-verified. Gates re-run on the r2 tree recorded below.
- Secret policy: do not read, print, persist, or commit credentials.
- Integration progress: mounted Task 4/5 relative subjects/cases/conversations routers under `workspace_router`; preserved Task 9 absolute analyses Charter/Run/Lens routes; added Report read/export routes; regenerated official OpenAPI/TypeScript contracts (50 paths) with `CONTRACT_DRIFT_OK`.
- Decision OS progress: added migration `c8d4e6f0a1b2` after `a7c3e9f1b5d8` for `decision_records`, `decision_reviews`, and append-only `decision_lifecycle_events`; `decision_records` and lifecycle events have DB append-only triggers; signoff routes only create DecisionRecord through the authorized human sign endpoint.
- Provider progress: wired `AnalysisWorker` role executors to the provider-neutral `ModelProvider` seam (`deepseek` live or explicit `fixture`), preserving the no-hidden-reasoning persistence boundary.
- Verification so far: ruff (`services/api/app` + release tests), compileall, targeted pytest (`test_release_integration_routes.py`, `test_models.py`, `test_decision_os_invariants.py`) = 37 passed; alembic single head/upgrade/check passed; official contract generation and `-Check` passed.
- Reconcile merge (2026-07-25): origin/main advanced cacf2a3 -> 465869a (A1+A2 joint wave, Task 11 B2, READ-01 wave incl. the c150d72 partial adoption + 86c6dc9 404 combination fix) merged back --no-ff. Resolutions: reports/routes.py = main (carries the 86c6dc9 fix; sole diff); tenancy/routes.py = mount UNION (main graph_reads/case-anchor + lane decisions_router); env.py = main (semantically identical); test_models.py + test_task0405_qa_battery.py = lane (decision-records superset: 13-file migration set, single head c8d4e6f0a1b2); test_evidence_models.py = both improvements (lane cwd-independent alembic.ini + main time-bomb comment); test_release_integration_routes.py = lane decision routes + main /content path expectation, migration pin made cwd-independent; HEAD/HISTORY append-only union via byte-safe python resolver (4+1 blocks, zero loss; displaced READ-01 main worklog archived verbatim). Contracts to be regenerated on the merged tree before the gate run.
