# Work Log: A1+A2 joint wave — quality gate + dossier QA mainline integration

- Status: COMPLETE on codex/task-10-0405-mainline-integration (fresh worktree decision-lab-task10-0405-mainline from main 23ab477, remote-verified echo); all post-merge gates green; pushed ff-only to main.
- Merge (1): no-ff adoption of codex/qa-task-10-quality-gate-r1 2bc4da72f064c339e7e4596151aaa14e45f324e5 = Task 10 quality-gate combo (82ee67c impl + 1f014b7 QA revision + adversarial battery; QA PASS 829/0; combination adopted as-is per dispatch, revisions NOT redone). Content conflicts: none beyond lifecycle.
- Merge (2): no-ff adoption of codex/qa-task-04-05-backend-r1 636831e84895be73a288a6a93e85772481b309ae (carries a51a4f9 impl + migration a7c3e9f1b5d8 + F1/F2 fixes). Semantic merges per A2 QA report integration notes: migrations/env.py keeps BOTH import blocks live (A2 dossiers F1 import placed before the A1 four-module Task 10 block); tests/test_models.py exact-table-set = union (A1 +6 Task 10 tables ∪ A2 +dossier_version_snapshots) with equality kept, A2 deterministic dossiers import auto-merged in the import header (F2). HEAD/HISTORY resolved append-only with byte-safe resolver both merges (merge-base prefix checks fail-closed; incoming worklogs already self-archived in their own HISTORY).
- Wave commit: QA-7 re-pinned to the merged chain (11-file versions set incl. b6e8f3a1d7c2; head pair collapses to single a7c3e9f1b5d8) per the A2 QA report integration note; test included in the green full suite below.
- Gates (all green, disposable PG16 ludus-pg-a1a2-wave @55460, real runs): alembic heads = single a7c3e9f1b5d8; upgrade head clean -> check "No new upgrade operations detected" (F1 semantic merge effective) -> downgrade -1 = b6e8f3a1d7c2 -> downgrade -1 = f9a4b7e2c8d3 -> re-upgrade head clean -> re-check clean. Full suite pytest tests app/simulations/tests app/evidence/tests app/analyses/tests -q -W error -rxX = 874 passed / 0 failed (new cross-wave baseline; comparable to A1 821-collected / A2 829-collected scopes; QA-7 green on merged tree; F3 self-healed - evidence suite zero failures); canonical two-dir command pytest tests app/simulations/tests -q -W error -rxX = 667 passed / 0 failed. pnpm --dir apps/web test = 68/68; build clean. generate_contracts.ps1 -Check = CONTRACT_DRIFT_OK. ruff all-pass; compileall exit 0; git diff --check clean; conflict-marker scan 0 on all changed files; scope audit = exactly the two lanes' declared write scopes + lifecycle + QA-7 re-pin; secret scan clean; HEAD/HISTORY append-only three-way preservation byte-verified (base prefix + A1 suffix + A2 suffix all kept).
- Next work must start from the latest local `main`, use a new `codex/<task-name>` branch, and replace this record before modifying task-owned files.
- Secret policy: do not record credentials here.
