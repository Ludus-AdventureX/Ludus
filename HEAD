# Work Log: CCR mount wave — deep-research pipeline route mounting & contract regeneration (A3, r1)

- Status: implementation COMPLETE on worktree decision-lab-ccr-mount-wave-r1; all wave gates green; pending commit + push (recorded on push).
- Role: Contract/Mainline Lead (combined lane); the only lane authorized to touch `app/main.py` and `packages/contracts` this wave.
- Branch: `codex/ccr-mount-wave-r1`, fresh worktree; parent = main @ `4941e58bee3b91f14a4a92b7fab92750ef85b3b6`. No rebase/amend/force-push/cherry-pick; main untouched.
- Gate 0: base `4941e58…` = live `git ls-remote origin refs/heads/main` EXACT match; deviation protocol not needed.
- Trigger: this is the A3 integration/mounting wave named by CCR-20260725-ANALYSIS-01-ADDENDUM-A1 §A1-4 (mount Charter/Run endpoints) + §A1-9 (canonicalize evidence read paths, then mount), plus Task 8 handoff §5.1 and Task 9 handoff §5.3.
- Adjudication (docs/product-plan/docs/contract-changes/CCR-20260726-MOUNT-01.md): M1 evidence read paths NEW+MOUNT (7 GET, closes A1-9); M2 analyses SSE/resolutions/cancel REAFFIRM+MOUNT (already canonical L71/74/75, closes A1-4 delivered subset); M3 Charter create/PATCH/replacements/confirm/runs DEFERRED (no HTTP handler on main); M4 GET run status DEFERRED (no handler); M5 strategic-lenses read ×2 DEFERRED (lens_artifact_reads is a read service, no router); M6 cases routes NOT-MOUNTED (A2 undelivered); M7 mount mechanism VARIANCE (owner routers ship absolute /api/workspaces/{workspaceId} prefix + per-route require_workspace_context, mounted on app directly — contract-equivalent, no owner code touched); M8 CSRF on resolutions/cancel VARIANCE (compliant under literal cookie-mutation scope; security follow-up flagged for auth lane); M9 Idempotency-Key on cancel VARIANCE (natural idempotency covers the semantic; owner follow-up); M10 app.types enum promotion NOT a mount blocker.
- Canonical text syncs (10-api-and-events.md): SM1 seven evidence read rows in the endpoint table; SM2 new "证据溯源与冲突读取 API" subsection (envelopes + DTO field sets + uniform CASE_NOT_FOUND + read-only posture).
- Mount manifest (app/main.py, +2 mounts, both schema-visible; guest router keeps include_in_schema=False, untouched): `app.include_router(evidence_router)` (7 GET) + `app.include_router(analyses_router)` (1 GET SSE + 2 POST).
- Contracts regenerated by the official scripts/generate_contracts.ps1 (uv + openapi-typescript 7.13.0): openapi.json + src/types.gen.ts. Published operations 8 → 18 (10 new; additive only, existing 8 byte-unchanged).
- Gates (fresh disposable PG16 ludus-pg-ccr-mount @55450, main-checkout venv via junction, uv/pnpm offline, zero network installs):
  - alembic upgrade head → single head `f9a4b7e2c8d3` exact.
  - Wave suite `pytest tests app/simulations/tests -q -W error -rxX` = 577 passed / 0 failed.
  - Owner suites (diagnostic) `pytest app/evidence/tests app/analyses/tests -q -W error -rxX` = 207 passed / 0 failed — the custom lazy `_IncludedRouter` publishes the mounted paths in app.openapi() while keeping the owner `app.routes`-based "router-not-mounted" assertions valid, so mounting is transparent to the owner suites (no owner test breakage; no owner code touched).
  - Functional smoke (ASGITransport): all four mounted paths serve and enforce auth/tenancy (401 SESSION_REVOKED_OR_EXPIRED envelope without a session); a bogus suffix returns bare Starlette 404 — confirming the four paths are registered.
  - Official `generate_contracts.ps1 -Check` = CONTRACT_DRIFT_OK (re-verified with real node_modules).
  - `ruff check services/api` all-pass; `compileall app migrations` exit 0; `git diff --check` clean (0 whitespace/conflict-marker); secret scan 0.
  - Web: `pnpm --dir apps/web test` (vitest run) = 41 passed / 0 failed; `pnpm --dir apps/web build` (next build --turbopack, incl. type-check against regenerated types.gen.ts) = success.
- Scope audit (git diff --name-only + status): exactly `docs/product-plan/10-api-and-events.md`, `packages/contracts/openapi.json`, `packages/contracts/src/types.gen.ts`, `services/api/app/main.py` (modified) + new `docs/product-plan/docs/contract-changes/CCR-20260726-MOUNT-01.md` + HEAD/HISTORY. ZERO diff to app/evidence, app/analyses, app/tenancy, other app code, tests, pnpm-lock.yaml, ways, apps/web source.
- ready_for_merge: YES (additive contract + mount; all wave gates green; owner code untouched).
- Broadcast: B1/B2/B3 front-end lanes — the canonical path list is finalized (10 published ops; see CCR mount manifest). Deferred (next wave): M3/M4 Charter+Run creation & run-status, M5 strategic-lenses read, M6 cases (A2), plus owner follow-ups M8 (CSRF on resolutions/cancel) / M9 (Idempotency-Key on cancel) / M10 (enum promotion).
- Next work must start from the latest remote-verified `main`, use a new `codex/<task-name>` branch, and replace this record before modifying task-owned files.
- Secret policy: do not record credentials here.
