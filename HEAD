# Work Log: analysis worker runner + deploy stack full-pipeline increment

- Status: ACTIVE on worktree decision-lab-deploy-stack; branch codex/prototype-deploy-stack, base = reconcile merge 8d7ed2e onto main 59b7dee. No rebase/amend/force.
- Scope: (1) NEW production entrypoint services/api/app/workers/run.py (python -m app.workers.run) - polls claim_next_queued, owns the session transaction (repo layer never commits), commits per processed run; poison-run parking = re-claim queue head (queued->planning) then planning->needs_attention (both edges state-machine legal), resumable via the shipped resolution API. (2) compose.prototype.yaml adds the worker service + MODEL_PROVIDER/MODEL_BASE_URL/MODEL_API_KEY/MODEL_NAME passthrough on api+worker. (3) .env.prototype.example documents the live-model 4-tuple and WORKER_POLL_INTERVAL_SECONDS.
- Bug fixed during verification: first parking attempt targeted the rolled-back run id directly (queued->needs_attention is ILLEGAL); observed as a poison loop in the deployed stack, fixed to the re-claim strategy above and re-verified live (run aa3f3b91 parked, worker quiet after).
- Gates: ruff + compileall on run.py clean; targeted pytest app/analyses/tests/test_analysis_worker.py + test_analysis_state_machine.py = 74/74 on disposable PG16 @54800; compose config valid; live E2E through the deployed stack (guest -> case -> charter create/confirm -> run create 201 -> worker claim -> fixture EmptyModelContentError -> parked needs_attention with last_resumable_stage=planning) - fixture-mode deep analysis parks by design (fixture provider has no registered stage outputs); live execution requires the MODEL_* 4-tuple in .env.prototype.
- Secret policy: do not record credentials here; MODEL_API_KEY lives only in gitignored .env.prototype.
