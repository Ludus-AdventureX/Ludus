# Work Log: alpha 体验修复会话（A/B/C 三层整改）

- Status: active
- Started: 2026-07-30 (Asia/Shanghai)
- Role: 交互式修复会话（用户驱动），单 owner
- Tree: `decision-lab-mainline-integration` on `main` @ 3eae639（本次改动暂不提交，
  留在工作树中由用户审阅；提交需用户明确授权）
- 部署对象: `ludus-alpha` compose 项目（compose.prototype.yaml + .env.prototype，
  web 映射 127.0.0.1:3200）
- 约束: 不 rebase/amend/force-push；不推远程；不新增依赖；秘密值不落本文件

## 背景（审计结论摘要，全文见本会话对话记录）

全流程实测（3200 端口）+ 日志/DB/源码追踪确认三条总根因：

1. 整栈运行在 FIXTURE_MODE=true / MODEL_PROVIDER=fixture（MODEL_API_KEY 已配
   但未启用）→ 对话/质检 502（EmptyModelContentError，API 侧 fixture provider
   无兜底）、focused 全为 [fixture] 占位、full 永远被 lens 审计拦截
   （strategic_lens_incomplete / strategic_lens_reference_mismatch）。
2. 新组件（Invite/Mentor/Signoff/FactorSandbox/Retro/Provenance/Portfolio/
   Progress/ReportList/clarifier）绕过 look 设计系统：look-styles.css 867 行后
   附加区引用不存在的 token（var(--hairline,#2a2a35) 等）落到暗色 hex fallback，
   圆角/渐变/pill/rem 违反 Look V7 合同；.signoff-panel 完全无 CSS（决定页表单
   错位重叠）。
3. 视图间无共享 Run/报告状态：report_artifacts 已写入但 J 页标题硬编码
   "报告尚未生成"、列表显示裸 UUID、详情为 JSON dump；E 页不消费 run
   （QualityGatePanel 为占位文案，quality_gate_results 表 0 行）；
   Q 页 .intro-grid align-items:end + 重组件塞入 intro-actions 槽导致启动分析
   时左栏整体下移。

## 任务规划

### A 层 — 让系统"真"起来（P0，先行）

- [ ] A1 环境切换: .env.prototype 设 MODEL_PROVIDER=deepseek、
      MODEL_BASE_URL=https://api.deepseek.com、MODEL_NAME=deepseek-v4-pro、
      FIXTURE_MODE=false（Key 已在，不记录值）；force-recreate api/worker；
      验证容器 env 与 worker 日志 provider 绑定。
- [ ] A2 后端诚实性: conversations 路由用户消息先持久化再调模型（模型失败不再
      吞消息、不再裸 502 文案）；fixture 部署下对话/质检绑定确定性兜底（与
      worker 的 synthesize_stage_response 同构）；前端 composer 错误文案改人话。
- [ ] A3 前端拦截语义: blocked reason_codes 映射人话+修复动作；originModes 含
      fixture 时 full 深度预先告知不可用（前端-only，不改 canonical 合同）。
- [ ] A4 验证: 浏览器实测 3200 —— 质检出卡、札记有系统回应、focused 真实检索
      产出、full 走真实五透镜；截图留档 output/；更新本文件。

### B 层 — 信息架构：让结果有去处（A 完成后）

- [ ] B1 J 判断页动态化：标题/状态由 report_artifacts 驱动；structured_content
      渲染为版式化报告（执行简报/条件化建议/证据引用/最强反对/六维质量/来源
      模式）；删除 JSON dump 与 UUID 按钮文案。
- [ ] B2 E 证据页接真数据：研究轨迹=analysis_events 回放；QualityGatePanel 消费
      quality_gate_results（管线补写 focused gate 行）+ blocked findings。
- [ ] B3 发起入口收敛：只留 Q 页发起，运行中引导至 E 页；删 E 页重复控件；
      决策组合墙状态与 run 同步。
- [ ] B4 布局修复：AnalysisLaunchPanel/进度流移出 view-intro；intro-actions 恢复
      轻量；重内容列 align-items 改 start；启动分析零位移。

### C 层 — 设计系统统一：去 AIGC 味（B 完成后）

- [ ] C1 重写 look-styles.css 867 行后附加区：全部消费既有 semantic token，
      方角/hairline/px/无渐变无阴影无 pill，形制对齐 ledger-sheet 族。
- [ ] C2 原生控件替换：slider/checkbox/radio 按 look 语言重绘。
- [ ] C3 决定页重构：无 qualifying full run 时签署区收起为状态说明；补 signoff
      样式；签署来源拒绝 focused run；导师评审从 D 页移除（如保留移入 Review）。
- [ ] C4 邀请协作者重做：look 风格 dialog；"授予签署权"独立二次确认。
- [ ] C5 文案清理：删除 Task 编号（Task 14W 等）、[fixture] 前缀改来源 badge、
      UUID 改问题标题+短编号、工程黑话改用户语言。
- [ ] C6 R 复盘 dialog 落地（Review canonical 结构后端已备）。
- [ ] C7 验收：Playwright 金路径 1440x900 + 390x844 截图人工审查；每视图
      loading/empty/error/blocked 状态核对。

## 已完成

- A0 归档：上一条 HEAD（mainline integration + alpha follow-ups 记录）已于
  2026-07-30 字节安全追加至 HISTORY（8485 bytes，前缀校验 PASS）。
- A1 环境切换（完成）：.env.prototype 已改 MODEL_PROVIDER=deepseek、
  MODEL_BASE_URL=https://api.deepseek.com、MODEL_NAME=deepseek-v4-pro、
  FIXTURE_MODE=false（Key 自 decision-lab/.env 程序化迁入，未回显）；
  另补 SIGNUP_INVITE_CODE_HASHES（本地测试邀请码的 SHA-256，码值不入档）。
  发现并处置：前序泣道已自行回收 ludus-alpha 栈（含 volume）并把
  alpha-followups 并入 main（tip 6142d04）；本次以新 main 重建镜像重拉栈。
- A1-附带（完成）：新 main 的生产镜像缺依赖——methods/source_validator 运行时
  import jsonschema，但 pyproject 把它放在 dev 组，api 容器启动即死
  （ModuleNotFoundError）。已将 jsonschema>=4.23,<5 移入 runtime
  dependencies 并 uv lock 重锁（零新增包，仅分组移动，lock 变化 4 行）。
- A2 后端诚实性（完成）：
  - conversations/routes.py：用户消息在调模型前先 commit（模型失败不再
    回滚札记，UI 的“已发送”不再说谎）；MODEL_OUTPUT_INVALID 文案改人话。
  - 新增 app/agents/conversation_fixtures.py：fixture 部署下对话/质检绑定
    确定性兜底（超集 payload 同时满足两套 schema，全部 [fixture] 前缀）；
    conversations.get_model_provider 与 cases.get_clarifier_provider 接入。
- A3 前端拦截语义（完成）：AnalysisLaunchPanel 的 blocked 卡点码映射人话
  （strategic_lens_* 四码）；trace 模型名以 fixture 开头时，blocked 引导改为
  “演示模式必然拦截，非档案缺口”，不再指向死循环的“回 Q 区补事实”。

## 验证记录

- 静态门：ruff + compileall 对三个后端改动文件全净；apps/web tsc --noEmit 全净。
- 部署门：docker compose build + up 成功；api healthy；/api/health 200；
  容器内 env 确认 MODEL_PROVIDER=deepseek、FIXTURE_MODE=false、Key len=35。
- 浏览器实测（3200，邀请码注册新账号+新案例）：
  1) 问题质检：真实模型返回完整质检卡（真决策/疑似假两难+第三选项/
     Type1/问题改写），“暂不可用”消失。
  2) 记入札记：系统回应真实生成（含待确认清单），structural validation
     错误消失。
  3) focused live 金路径：Run 6e558ff4 六阶段 queued→…→ready（约 6 分钟），
     origin_modes={live}，每阶段带 deepseek-v4-pro 标识与真实内容；
     report_artifacts 写入真实报告（qualityGate.passed=true，证据带真实 URL，
     来源分级 L5/L6）。注：EXA/FIRECRAWL 无 Key，检索为模型自主检索降级路径。
  4) full live：六阶段全部真实执行后仍 blocked@0.86，卡点
     strategic_lens_incomplete + reference_mismatch，strategic_lens_artifacts
     为 0 行 —— 见下方新发现 P0。

## 新发现 P0（A 层验证过程中暴露，超出 A 层原定范围）

- **full 深度对 live 也是结构性死路**：worker 的通用 stage prompt 只声明
  "lensPayloads 没有就给 {}"，从未按方法包的五透镜独立 prompt/schema 发起
  专项调用；live 模型自然返回 {} → 五透镜零持久化 → lens 审计必拦。
  基建全部已存在但未接线：method-packs/.../prompts/lenses/*.md、
  strategic-lens-output.schema.json、app/strategic_lenses/（五 lens 实现+
  validators+repository 写路径）、worker 的 FULL_LENS_SCHEDULE/_run_lens_stages
  消费侧。缺的是：_run_lens_stages 在 payload 缺失时按包内 prompt+schema 逐
  lens 调模型的执行器（owner 归属 Ways/Agent Pipeline 泳道）。
- 处置：已向用户上报，等待决策（立即修 六 B/C 后修 六 另开泳道）。
  A3 的 blocked 引导已避免死循环文案，但 live 下的卡点仍会被误读为
  "证据不足"，需在修复前临时标注或尽快修复。

## 未验证 / 风险

- 上一条 HEAD 声称 alpha 栈已停止移除且 invite gate 已并入 main，但实测
  ludus-alpha 五容器在跑、main tip 仍为 3eae639、guest 入口开启——记录与现实
  不一致，本次以现实为准，不追溯改写历史记录。
- 本机另有 3000/8000 本地进程（decision-lab-worktrees/analysis-progress-ui），
  易与 3200 混淆；本会话只动 3200 栈。
- 切 live 后 DeepSeek 调用产生真实费用；EXA/FIRECRAWL key 状态待 A1 时核验。

## 下一步

- 用户自选模型 + 自定义 MCP 连接器已完成并部署。
  - workspace_connectors 表已扩展：provider 支持 exa/tavily/firecrawl/model/mcp，新增 config JSONB 列。
  - 连接器设置面板支持三种类型：搜索连接器、自定义模型、MCP 工具服务器。
  - 对话/分析优先使用 workspace 自定义模型，无则回落系统默认 DeepSeek。
  - MCP 工具发现端点 GET /connectors/{id}/tools 已实现。
- 剩余工作：
  - P1: Worker MCP 工具调用集成（深度分析 retrieving 阶段可调用 MCP 工具）
  - full 五透镜 evidence ledger 集成（另开泳道）
