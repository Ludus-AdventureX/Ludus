# Work Log: live deep-analysis execution hardening (deploy lane)

- Status: ACTIVE on worktree decision-lab-deploy-stack; branch codex/prototype-deploy-stack. No rebase/amend/force.
- Context: with FIXTURE_MODE=false + DeepSeek MODEL_* set, the deep-analysis worker ran live but the multi-stage run failed. Two real defects found and fixed by live E2E through the deployed stack.
- Fix 1 (model_provider.py DeepSeekModelProvider._client): default httpx transport had retries=0, so an intermittent cold-TLS ConnectError from the provider killed a whole multi-stage run. Now builds httpx.AsyncHTTPTransport(retries=3) when no test transport is injected (connection-establishment retries only, idempotent, never replays a partial request).
- Fix 2 (analysis_worker.py): live models emit `output` as a summary string and `lensPayloads` as [] instead of {}. The executor already normalizes both before persistence, but the pre-persistence _STAGE_RESULT_SCHEMA rejected them. Relaxed output to object|string and lensPayloads to object|array (module-local executor gate only, NOT a canonical DB/API contract), and hardened the stage executor system prompt with explicit per-key value types + an example.
- Live verification (deployed stack, deepseek-v4-pro, real HTTP): guest -> case -> charter confirm -> run create -> worker claimed and drove ALL focused stages (planning/retrieving/analyzing/criticizing/synthesizing/validating) -> terminal `blocked` at progress 0.86 (quality-gate fail-closed terminal, lastResumableStage=null). Worker log: "processed analysis run ..." with zero ConnectError/SchemaValidationError. Balance endpoint confirmed the key valid (available). DeepSeek official model name for this account = deepseek-v4-pro / deepseek-v4-flash (deepseek-chat/reasoner rejected 400).
- Gates: ruff + compileall clean on both changed files; live E2E as above.
- Secret policy: MODEL_API_KEY lives only in gitignored .env.prototype; never recorded here.
